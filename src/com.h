#ifndef COM_H
#define COM_H
/*
#include "helper.h"
#include <EEPROM.h> 

int buffer[3]; //Serial receive data buffer
int rec_flag; // serial port receiving flag
int serial_data; // serial data zero time storage
unsigned long Costtime; // serial timeout count
int IR_R; //Infrared status flag on the right side of the line
int IR_L; //Infrared status flag on the left side of the line
int IR; //intermediate infrared status flag

void setup_com();
void get_data();
void communication_decode(int i);
*/
#endif
